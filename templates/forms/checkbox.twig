<div class="check-box">
    <input type="checkbox" id="{{ id }}" name="{{ name }}" {% if isChecked is defined and isChecked %} checked {% endif %}>
    <label for="{{ id }}">{{ message }}</label>
</div>